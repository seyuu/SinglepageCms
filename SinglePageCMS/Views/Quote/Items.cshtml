@{
    Layout = "~/Views/Admin/_Layout.cshtml";
}

<div class="block-header">
    <div class="row remove-margin">
        <div class="col-md-10">
            <div class="header-section">
                <h1>
                    Dipnotlar<br />
                    <small></small>
                </h1>
            </div>
        </div>
        <div class="col-md-2">
            <a href="~/admin/quote/itemadd/@ViewBag.quoteID" class="header-link">
                <h1 class="animation-pullDown">
                    <i class="fa fa-plus animation-expandUp"></i><br><small>Ekle</small>
                </h1>
            </a>
        </div>
    </div>
</div>

<div class="table-responsive">
    <table class="table table-bordered table-striped">
        <thead>
            <tr>
                <th>İsim</th>
                <th>Başlık</th>
                <th>Aciklama</th>
                <th class="text-center">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var i in ViewBag.model) {
                <tr>
                    <td>@i.Name</td>
                    <td>@i.Title</td>
                    <td>@i.Description</td>
                    <td class="text-center">
                        <div class="btn-group btn-group-sm">
                            <a href="~/admin/quote/itemedit/@i.ID" class="btn btn-success"><i class="fa fa-pencil"></i> Düzenle</a>
                            <a href="~/admin/quote/itemdelete/@i.ID" class="btn btn-danger" onclick="return confirm('Kaydı silmek istediğinize emin misiniz?')"><i class="fa fa-times"></i> Sil</a>
                        </div>
                    </td>
                </tr>

            }
        </tbody>
    </table>
</div>